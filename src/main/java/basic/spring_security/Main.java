package basic.spring_security;

public class Main {
    //todo use started to edit config instead of create from scratch
    // 2 default user + generated password
    // 3 wow we can see session after login
    // 4 curl -> dispatcher servlet -> call map(path, controller) -> redirect to controller
    // 5 filter chaining google cases
    // 6 password encoder
    // 7 last
    // 8 roles
}
